---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "kubebackup.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ template "kubebackup.name" . }}"
    chart: "{{ template "kubebackup.chart" . }}"
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
